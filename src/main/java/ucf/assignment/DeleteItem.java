package ucf.assignment;

import javafx.scene.control.TableView;

import java.nio.file.Paths;

public class DeleteItem
{
    TableView tableView;

    TableView deleteItem()
    {
        //This function will be called when pressed the Delete item in the viewList.fxml
        //This will permanently remove the item if it is checked
        //also needs to delete it from list
        return tableView;
    }
}
